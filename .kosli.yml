version: 1

trail:
  attestations:
    - name: pull-request
      type: pull_request
  artifacts:
    - name: exercises-start-points
      attestations:
        - name: provenance
          type: decision

        - name: snyk-container-scan
          type: decision
